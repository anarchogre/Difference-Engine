# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 13:41 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1339_CDT.md`

## MATERIAL CHANGE

The latest source probe did not expose the mutation sites for the three counters.

It did prove additional structure:

- `reset()` explicitly zeros `n_prompt_tokens_cache`.
- `copy_state_to()` copies:
  - `n_decoded`
  - `n_prompt_tokens_cache`
  - `n_prompt_tokens_processed`
  - cloned `prompt` state
- server metrics keep prompt evaluation and prediction accounting separate:
  - prompt evaluation uses `slot.n_prompt_tokens_processed`
  - prediction uses `slot.n_decoded`
- release still reports `prompt.n_tokens()`.

Wolfram confirms:
- prompt and prediction metrics are separate;
- generation counter is `n_decoded`;
- release counter is `prompt.n_tokens()`;
- the supplied excerpt contains no actual increment/assignment site for `n_prompt_tokens_processed` or `n_decoded`.

## INTERPRETATION

The grep pattern was too narrow to locate the mutation sites. This is a search failure, not evidence that the counters are not updated.

No new causal claim is promoted.

## NEXT

Search the complete source tree for every reference to:
- `n_prompt_tokens_processed`
- `n_prompt_tokens_cache`
- `n_decoded`

Then inspect the actual mutation sites only.

**Reality is final validator.**
