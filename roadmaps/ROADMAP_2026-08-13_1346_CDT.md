# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 13:46 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1344_CDT.md`

## MATERIAL CHANGE

The exact prompt-completion boundary in llama-server is now source-resolved.

In `tools/server/server-context.cpp`:

- prompt completion is detected only when:
  `slot.prompt.n_tokens() == slot.task->n_tokens()`
- at that boundary:
  - `slot.state = SLOT_STATE_DONE_PROMPT`
  - output logits are enabled for the final prompt token
  - `slot.n_decoded = 0`
  - `slot.i_batch` is set
  - sampler is initialized

Therefore generation accounting begins only after the entire prompt has been processed and the slot transitions to `SLOT_STATE_DONE_PROMPT`.

Wolfram confirms:
- prompt-done condition: prompt token state equals total task tokens
- state transition: `SLOT_STATE_DONE_PROMPT`
- generation counter reset: `n_decoded = 0`
- generation for task 3142 is not proven by the supplied evidence

## INTERPRETATION

For task 3142, the logs still show only:
`TASK LAUNCH -> PROMPT PROCESSING CHECKPOINT -> CANCEL -> RELEASE`

There is no visible `n_decoded` progress line, prompt-completion timing line, or response output.

The source tells us what generation would require, but the current task evidence does not prove that the prompt ever reached the completion boundary before OpenClaw canceled it.

## NEXT

Inspect the server log around task 3142 without filtering only by task id, looking for slot-id-1 state-transition, decode, timing, or prompt-completion lines that may omit the task number.

Target interval:
13:16:00–13:17:22 CDT.

**Reality is final validator.**
