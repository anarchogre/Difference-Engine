# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 12:35 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1231_CDT.md`

## MATERIAL CHANGE

Latest source/config evidence confirms:
- Pan provider model entry exists with id `pan-qwen`.
- that entry declares `contextWindow = 16384`.
- memory preflight obtains its context through `resolveMemoryFlushContextWindowTokens()`, which delegates to `resolveContextTokensForModel(provider, model)`.
- follow-up provider and model identity are resolved dynamically before that lookup.

Wolfram formal verification:
- if `resolveContextTokensForModel` actually finds the configured 16384-token model window, the resolved context cannot exceed 24000.
- under a positive agent cap, the resolved value is at most `min(agentCap, 16384)`.
- a >24000 result is possible only when the 16384 model window is not found and a fallback/trusted window greater than 24000 survives any agent cap.

Therefore the current leading failure hypothesis is now precise:

**MODEL-WINDOW LOOKUP MISS / PROVIDER-MODEL IDENTITY MISMATCH**

Not yet proven:
- the exact implementation and precedence inside `resolveContextTokensForModel`;
- the exact string returned by `resolveFollowupContextConfigProvider`;
- the exact provider/model pair passed during v4;
- the actual v4 `preflightCompaction check` contextWindow value.

## NEXT

Read-only:
1. locate the actual bundled definition/import source of `resolveContextTokensForModel`;
2. locate `resolveFollowupContextConfigProvider`;
3. capture their complete source windows;
4. capture runtime v4 provider/model/context/threshold lines if logged;
5. compare those IDs against configured `pan-local` / `pan-qwen`.

Only then mutate configuration.

## PHASE

`MODEL-WINDOW LOOKUP MISS PROOF → CONTEXT REPAIR → RESPONSE-LIFECYCLE REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
