# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 14:12 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1407_CDT.md`

## MATERIAL CHANGE

The second v7 launcher attempt also failed before OpenClaw runtime initialization.

Evidence:
- `acceptance.json` size = 87 bytes
- captured error:
  `env: 'node': No such file or directory`
  `env: use -[v]S to pass options in shebang lines`

Classification:
`LAUNCHER_FAILURE_BEFORE_OPENCLAW_NODE_PATH`

Wolfram confirms:
- OpenClaw started: false
- model run occurred: false
- root layer: the OpenClaw CLI shim's `/usr/bin/env node` resolution depends on current shell PATH
- deterministic launcher: invoke the managed Node binary directly with the absolute `openclaw.mjs` path

## INTERPRETATION

Using the absolute OpenClaw shim path did not remove PATH dependence because the shim itself dispatches through `/usr/bin/env node`.

Do not:
- install Node globally
- install OpenClaw via Snap
- alter global PATH merely to satisfy this test

Instead:
- use the already-managed Node runtime under the OpenClaw tree;
- call `openclaw.mjs` directly;
- preserve the failed 87-byte capture before the next retry.

Relative to the real v6 OpenClaw run, the intended experimental delta remains only:
`--timeout 180 -> --timeout 600`.

## NEXT

Verify the managed Node binary directly, then run v7 through:

`managed node -> openclaw.mjs -> agent`

**Reality is final validator.**
