# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 12:21 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1219_CDT.md`

## MATERIAL CHANGE

Wolfram verified the condition required for the observed positive embedded preflight threshold.

For:
`threshold = max(16384 - reserveTokensFloor - softThresholdTokens, serverCompactionThreshold)`

with all inputs nonnegative, threshold is positive iff either:

1. `reserveTokensFloor + softThresholdTokens < 16384`, or
2. `serverCompactionThreshold > 0`.

Therefore, with the raw fallback values:
- reserveTokensFloor = 20000
- softThresholdTokens = 4000

the observed `reason=threshold` compaction is impossible unless:
- `memoryFlushPlan` supplied smaller effective values whose sum is below 16384, or
- `serverCompactionThreshold` was positive.

This reduces the remaining diagnosis to two concrete branches.

## NEXT

Inspect only:
- exact `resolveMemoryFlushPlan` implementation and current `memoryFlush` config
- exact `resolveResponsesServerCompactionThreshold` implementation and current Pan model compat/extra-parameter values

Then select the proven branch and repair only that branch.

## PHASE

`TWO-BRANCH THRESHOLD RESOLUTION → RESPONSE-LIFECYCLE REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
