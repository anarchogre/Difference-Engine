# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 13:10 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1307_CDT.md`

## MATERIAL CHANGE

The installed OpenClaw embedded-run source now shows the configuration application sequence around the prepared SettingsManager:

1. `settingsManager = createPreparedEmbeddedAgentSettingsManager(...)`
2. `applyAgentAutoCompactionGuard(...)`
3. `applyAgentCompactionSettingsFromConfig({ settingsManager, ... })`
4. `applyAgentAutoCompactionGuard(...)` again

This strongly supports a deliberate control-plane path from agent compaction configuration into the same SettingsManager consumed by the core compaction threshold logic.

However, the currently captured evidence does not yet include the definition of `applyAgentCompactionSettingsFromConfig`, so the exact field mapping from `agents.defaults.compaction.reserveTokens` to `settingsManager.settings.compaction.reserveTokens` is not yet directly proven.

## WOLFRAM CHECK

For Pan's fixed context window:

`W = 16384`

Core auto-compaction threshold:

`T > W - R`

A positive threshold exists iff integer reserve satisfies:

`0 <= R <= 16383`

Candidate arithmetic only:
- `R = 2048` → threshold `14336` (12.5% reserve)
- `R = 4096` → threshold `12288` (25% reserve)

No candidate is promoted yet.

## ROOT CAUSE STATUS

Validated:
- core default reserve = 16384
- Pan context window = 16384
- no persisted override found by targeted census
- default threshold = 0
- every positive context triggers core auto-compaction
- v4 tool execution and final `STATUS=PASS` generation both succeeded before compaction timeout

Remaining pre-mutation proof:
- exact `applyAgentCompactionSettingsFromConfig` field mapping and accepted control-plane key

## NEXT

One final source-window proof:

1. locate `applyAgentCompactionSettingsFromConfig` definition/import source;
2. capture its body;
3. prove whether it maps `agents.defaults.compaction.reserveTokens` to `settingsManager`;
4. inspect schema-valid control-plane path;
5. preserve config;
6. apply bounded reserve repair;
7. fresh actuator acceptance requiring clean final return.

## CURRENT EDGE

`MODEL → PAN → EXEC → FORGE → RESULT → PAN FINAL TEXT = PASS`

Blocked:
`CORE DEFAULT RESERVE == CONTEXT WINDOW → THRESHOLD 0 → AUTO-COMPACTION TIMEOUT`

## PHASE

`FIELD-BINDING PROOF → RESERVE REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
