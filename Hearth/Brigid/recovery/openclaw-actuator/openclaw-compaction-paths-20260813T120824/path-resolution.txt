
FILE=/home/reckless-ideator/.openclaw/tools/node-v24.15.0/lib/node_modules/openclaw/dist/agent-runner.runtime-DtdxZiBX.js
HIT=3131: 	return resolvePositiveIntegerParam(sources, "responsesCompactThreshold");
3117: 	const modelId = params.modelId?.trim();
3118: 	if (!provider || !modelId) return;
3119: 	const legacyKey = legacyModelKey(provider, modelId);
3120: 	const providerConfig = params.cfg?.models?.providers?.[provider];
3121: 	const modelConfig = params.cfg?.agents?.defaults?.models?.[modelKey(provider, modelId)] ?? (legacyKey ? params.cfg?.agents?.defaults?.models?.[legacyKey] : void 0);
3122: 	const providerModelConfig = providerConfig?.models?.find((entry) => entry.id === modelId);
3123: 	const sources = [
3124: 		asRecord(providerConfig?.params),
3125: 		asRecord(providerModelConfig?.params),
3126: 		asRecord(params.cfg?.agents?.defaults?.params),
3127: 		asRecord(modelConfig?.params)
3128: 	];
3129: 	const serverCompaction = resolveBooleanParam(sources, "responsesServerCompaction");
3130: 	if (!(provider === "openai" ? serverCompaction !== false : serverCompaction === true)) return;
3131: 	return resolvePositiveIntegerParam(sources, "responsesCompactThreshold");
3132: }
3133: function resolveMemoryFlushGateState(params) {
3134: 	if (!params.entry) return null;
3135: 	const totalTokens = resolvePositiveTokenCount(params.tokenCount) ?? resolveFreshSessionTotalTokens(params.entry);
3136: 	if (!totalTokens || totalTokens <= 0) return null;
3137: 	const contextWindow = Math.max(1, Math.floor(params.contextWindowTokens));
3138: 	const reserveTokens = Math.max(0, Math.floor(params.reserveTokensFloor));
3139: 	const softThreshold = Math.max(0, Math.floor(params.softThresholdTokens));
3140: 	const threshold = Math.max(0, contextWindow - reserveTokens - softThreshold, Math.floor(params.minimumThresholdTokens ?? 0));
3141: 	if (threshold <= 0) return null;
3142: 	return {
3143: 		entry: params.entry,
3144: 		totalTokens,
3145: 		threshold
---

FILE=/home/reckless-ideator/.openclaw/tools/node-v24.15.0/lib/node_modules/openclaw/dist/agent-settings-axYuScuh.js
HIT=29: 	let maxReserveTokens;
15: 	return Math.floor(value);
16: }
17: function toPositiveInt(value) {
18: 	if (typeof value !== "number" || !Number.isFinite(value) || value <= 0) return;
19: 	return Math.floor(value);
20: }
21: /** Applies configured compaction reserve/keep-recent settings to an agent settings manager. */
22: function applyAgentCompactionSettingsFromConfig(params) {
23: 	const currentReserveTokens = params.settingsManager.getCompactionReserveTokens();
24: 	const currentKeepRecentTokens = params.settingsManager.getCompactionKeepRecentTokens();
25: 	const compactionCfg = params.cfg?.agents?.defaults?.compaction;
26: 	const configuredReserveTokens = toNonNegativeInt(compactionCfg?.reserveTokens);
27: 	const configuredKeepRecentTokens = toPositiveInt(compactionCfg?.keepRecentTokens);
28: 	let reserveTokensFloor = resolveCompactionReserveTokensFloor(params.cfg);
29: 	let maxReserveTokens;
30: 	const contextTokenBudget = toPositiveInt(params.contextTokenBudget);
31: 	if (contextTokenBudget !== void 0) {
32: 		const minPromptBudget = Math.min(MIN_PROMPT_BUDGET_TOKENS, Math.max(1, Math.floor(contextTokenBudget * MIN_PROMPT_BUDGET_RATIO)));
33: 		maxReserveTokens = Math.max(0, contextTokenBudget - minPromptBudget);
34: 		reserveTokensFloor = Math.min(reserveTokensFloor, maxReserveTokens);
35: 	}
36: 	let targetReserveTokens = Math.max(configuredReserveTokens ?? currentReserveTokens, reserveTokensFloor);
37: 	if (maxReserveTokens !== void 0) targetReserveTokens = Math.min(targetReserveTokens, maxReserveTokens);
38: 	const targetKeepRecentTokens = configuredKeepRecentTokens ?? currentKeepRecentTokens;
39: 	const overrides = {};
40: 	if (targetReserveTokens !== currentReserveTokens) overrides.reserveTokens = targetReserveTokens;
41: 	if (targetKeepRecentTokens !== currentKeepRecentTokens) overrides.keepRecentTokens = targetKeepRecentTokens;
42: 	const didOverride = Object.keys(overrides).length > 0;
43: 	if (didOverride) params.settingsManager.applyOverrides({ compaction: overrides });
---

FILE=/home/reckless-ideator/.openclaw/tools/node-v24.15.0/lib/node_modules/openclaw/dist/agent-settings-axYuScuh.js
HIT=33: 		maxReserveTokens = Math.max(0, contextTokenBudget - minPromptBudget);
19: 	return Math.floor(value);
20: }
21: /** Applies configured compaction reserve/keep-recent settings to an agent settings manager. */
22: function applyAgentCompactionSettingsFromConfig(params) {
23: 	const currentReserveTokens = params.settingsManager.getCompactionReserveTokens();
24: 	const currentKeepRecentTokens = params.settingsManager.getCompactionKeepRecentTokens();
25: 	const compactionCfg = params.cfg?.agents?.defaults?.compaction;
26: 	const configuredReserveTokens = toNonNegativeInt(compactionCfg?.reserveTokens);
27: 	const configuredKeepRecentTokens = toPositiveInt(compactionCfg?.keepRecentTokens);
28: 	let reserveTokensFloor = resolveCompactionReserveTokensFloor(params.cfg);
29: 	let maxReserveTokens;
30: 	const contextTokenBudget = toPositiveInt(params.contextTokenBudget);
31: 	if (contextTokenBudget !== void 0) {
32: 		const minPromptBudget = Math.min(MIN_PROMPT_BUDGET_TOKENS, Math.max(1, Math.floor(contextTokenBudget * MIN_PROMPT_BUDGET_RATIO)));
33: 		maxReserveTokens = Math.max(0, contextTokenBudget - minPromptBudget);
34: 		reserveTokensFloor = Math.min(reserveTokensFloor, maxReserveTokens);
35: 	}
36: 	let targetReserveTokens = Math.max(configuredReserveTokens ?? currentReserveTokens, reserveTokensFloor);
37: 	if (maxReserveTokens !== void 0) targetReserveTokens = Math.min(targetReserveTokens, maxReserveTokens);
38: 	const targetKeepRecentTokens = configuredKeepRecentTokens ?? currentKeepRecentTokens;
39: 	const overrides = {};
40: 	if (targetReserveTokens !== currentReserveTokens) overrides.reserveTokens = targetReserveTokens;
41: 	if (targetKeepRecentTokens !== currentKeepRecentTokens) overrides.keepRecentTokens = targetKeepRecentTokens;
42: 	const didOverride = Object.keys(overrides).length > 0;
43: 	if (didOverride) params.settingsManager.applyOverrides({ compaction: overrides });
44: 	return {
45: 		didOverride,
46: 		compaction: {
47: 			reserveTokens: targetReserveTokens,
---

FILE=/home/reckless-ideator/.openclaw/tools/node-v24.15.0/lib/node_modules/openclaw/dist/agent-settings-axYuScuh.js
HIT=34: 		reserveTokensFloor = Math.min(reserveTokensFloor, maxReserveTokens);
20: }
21: /** Applies configured compaction reserve/keep-recent settings to an agent settings manager. */
22: function applyAgentCompactionSettingsFromConfig(params) {
23: 	const currentReserveTokens = params.settingsManager.getCompactionReserveTokens();
24: 	const currentKeepRecentTokens = params.settingsManager.getCompactionKeepRecentTokens();
25: 	const compactionCfg = params.cfg?.agents?.defaults?.compaction;
26: 	const configuredReserveTokens = toNonNegativeInt(compactionCfg?.reserveTokens);
27: 	const configuredKeepRecentTokens = toPositiveInt(compactionCfg?.keepRecentTokens);
28: 	let reserveTokensFloor = resolveCompactionReserveTokensFloor(params.cfg);
29: 	let maxReserveTokens;
30: 	const contextTokenBudget = toPositiveInt(params.contextTokenBudget);
31: 	if (contextTokenBudget !== void 0) {
32: 		const minPromptBudget = Math.min(MIN_PROMPT_BUDGET_TOKENS, Math.max(1, Math.floor(contextTokenBudget * MIN_PROMPT_BUDGET_RATIO)));
33: 		maxReserveTokens = Math.max(0, contextTokenBudget - minPromptBudget);
34: 		reserveTokensFloor = Math.min(reserveTokensFloor, maxReserveTokens);
35: 	}
36: 	let targetReserveTokens = Math.max(configuredReserveTokens ?? currentReserveTokens, reserveTokensFloor);
37: 	if (maxReserveTokens !== void 0) targetReserveTokens = Math.min(targetReserveTokens, maxReserveTokens);
38: 	const targetKeepRecentTokens = configuredKeepRecentTokens ?? currentKeepRecentTokens;
39: 	const overrides = {};
40: 	if (targetReserveTokens !== currentReserveTokens) overrides.reserveTokens = targetReserveTokens;
41: 	if (targetKeepRecentTokens !== currentKeepRecentTokens) overrides.keepRecentTokens = targetKeepRecentTokens;
42: 	const didOverride = Object.keys(overrides).length > 0;
43: 	if (didOverride) params.settingsManager.applyOverrides({ compaction: overrides });
44: 	return {
45: 		didOverride,
46: 		compaction: {
47: 			reserveTokens: targetReserveTokens,
48: 			keepRecentTokens: targetKeepRecentTokens
---

FILE=/home/reckless-ideator/.openclaw/tools/node-v24.15.0/lib/node_modules/openclaw/dist/agent-settings-axYuScuh.js
HIT=37: 	if (maxReserveTokens !== void 0) targetReserveTokens = Math.min(targetReserveTokens, maxReserveTokens);
23: 	const currentReserveTokens = params.settingsManager.getCompactionReserveTokens();
24: 	const currentKeepRecentTokens = params.settingsManager.getCompactionKeepRecentTokens();
25: 	const compactionCfg = params.cfg?.agents?.defaults?.compaction;
26: 	const configuredReserveTokens = toNonNegativeInt(compactionCfg?.reserveTokens);
27: 	const configuredKeepRecentTokens = toPositiveInt(compactionCfg?.keepRecentTokens);
28: 	let reserveTokensFloor = resolveCompactionReserveTokensFloor(params.cfg);
29: 	let maxReserveTokens;
30: 	const contextTokenBudget = toPositiveInt(params.contextTokenBudget);
31: 	if (contextTokenBudget !== void 0) {
32: 		const minPromptBudget = Math.min(MIN_PROMPT_BUDGET_TOKENS, Math.max(1, Math.floor(contextTokenBudget * MIN_PROMPT_BUDGET_RATIO)));
33: 		maxReserveTokens = Math.max(0, contextTokenBudget - minPromptBudget);
34: 		reserveTokensFloor = Math.min(reserveTokensFloor, maxReserveTokens);
35: 	}
36: 	let targetReserveTokens = Math.max(configuredReserveTokens ?? currentReserveTokens, reserveTokensFloor);
37: 	if (maxReserveTokens !== void 0) targetReserveTokens = Math.min(targetReserveTokens, maxReserveTokens);
38: 	const targetKeepRecentTokens = configuredKeepRecentTokens ?? currentKeepRecentTokens;
39: 	const overrides = {};
40: 	if (targetReserveTokens !== currentReserveTokens) overrides.reserveTokens = targetReserveTokens;
41: 	if (targetKeepRecentTokens !== currentKeepRecentTokens) overrides.keepRecentTokens = targetKeepRecentTokens;
42: 	const didOverride = Object.keys(overrides).length > 0;
43: 	if (didOverride) params.settingsManager.applyOverrides({ compaction: overrides });
44: 	return {
45: 		didOverride,
46: 		compaction: {
47: 			reserveTokens: targetReserveTokens,
48: 			keepRecentTokens: targetKeepRecentTokens
49: 		}
50: 	};
51: }
---

FILE=/home/reckless-ideator/.openclaw/tools/node-v24.15.0/lib/node_modules/openclaw/dist/plugin-sdk/config-schema.d.ts
HIT=938:         reserveTokensFloor: ZodOptional<ZodNumber>;
924:           maxChars: ZodOptional<ZodNumber>;
925:           headChars: ZodOptional<ZodNumber>;
926:           tailChars: ZodOptional<ZodNumber>;
927:         }, $strict>>;
928:         hardClear: ZodOptional<ZodObject<{
929:           enabled: ZodOptional<ZodBoolean>;
930:           placeholder: ZodOptional<ZodString>;
931:         }, $strict>>;
932:       }, $strict>>;
933:       compaction: ZodOptional<ZodObject<{
934:         mode: ZodOptional<ZodUnion<readonly [ZodLiteral<"default">, ZodLiteral<"safeguard">]>>;
935:         provider: ZodOptional<ZodString>;
936:         reserveTokens: ZodOptional<ZodNumber>;
937:         keepRecentTokens: ZodOptional<ZodNumber>;
938:         reserveTokensFloor: ZodOptional<ZodNumber>;
939:         maxHistoryShare: ZodOptional<ZodNumber>;
940:         customInstructions: ZodOptional<ZodString>;
941:         identifierPolicy: ZodOptional<ZodUnion<readonly [ZodLiteral<"strict">, ZodLiteral<"off">, ZodLiteral<"custom">]>>;
942:         identifierInstructions: ZodOptional<ZodString>;
943:         recentTurnsPreserve: ZodOptional<ZodNumber>;
944:         qualityGuard: ZodOptional<ZodObject<{
945:           enabled: ZodOptional<ZodBoolean>;
946:           maxRetries: ZodOptional<ZodNumber>;
947:         }, $strict>>;
948:         midTurnPrecheck: ZodOptional<ZodObject<{
949:           enabled: ZodOptional<ZodBoolean>;
950:         }, $strict>>;
951:         postIndexSync: ZodOptional<ZodEnum<{
952:           off: "off";
---

FILE=/home/reckless-ideator/.openclaw/tools/node-v24.15.0/lib/node_modules/openclaw/dist/plugin-sdk/config-schema.d.ts
HIT=962:           softThresholdTokens: ZodOptional<ZodNumber>;
948:         midTurnPrecheck: ZodOptional<ZodObject<{
949:           enabled: ZodOptional<ZodBoolean>;
950:         }, $strict>>;
951:         postIndexSync: ZodOptional<ZodEnum<{
952:           off: "off";
953:           async: "async";
954:           await: "await";
955:         }>>;
956:         postCompactionSections: ZodOptional<ZodArray<ZodString>>;
957:         model: ZodOptional<ZodString>;
958:         timeoutSeconds: ZodOptional<ZodNumber>;
959:         memoryFlush: ZodOptional<ZodObject<{
960:           enabled: ZodOptional<ZodBoolean>;
961:           model: ZodOptional<ZodString>;
962:           softThresholdTokens: ZodOptional<ZodNumber>;
963:           forceFlushTranscriptBytes: ZodOptional<ZodUnion<readonly [ZodNumber, ZodString]>>;
964:           prompt: ZodOptional<ZodString>;
965:           systemPrompt: ZodOptional<ZodString>;
966:         }, $strict>>;
967:         truncateAfterCompaction: ZodOptional<ZodBoolean>;
968:         maxActiveTranscriptBytes: ZodOptional<ZodUnion<readonly [ZodNumber, ZodString]>>;
969:         notifyUser: ZodOptional<ZodBoolean>;
970:       }, $strict>>;
971:       runRetries: ZodOptional<ZodObject<{
972:         base: ZodOptional<ZodNumber>;
973:         perProfile: ZodOptional<ZodNumber>;
974:         min: ZodOptional<ZodNumber>;
975:         max: ZodOptional<ZodNumber>;
976:       }, $strict>>;
---

FILE=/home/reckless-ideator/.openclaw/tools/node-v24.15.0/lib/node_modules/openclaw/dist/extensions/memory-core/index.js
HIT=79: 	const softThresholdTokens = normalizeNonNegativeInt(defaults?.softThresholdTokens) ?? 4e3;
65: 	return next;
66: }
67: function appendCurrentTimeLine(text, timeLine) {
68: 	const trimmed = text.trimEnd();
69: 	if (!trimmed) return timeLine;
70: 	if (trimmed.includes("Current time:")) return trimmed;
71: 	return `${trimmed}\n${timeLine}`;
72: }
73: function buildMemoryFlushPlan(params = {}) {
74: 	const resolved = params;
75: 	const nowMs = resolveMemoryCoreNowMs(resolved.nowMs);
76: 	const cfg = resolved.cfg;
77: 	const defaults = cfg?.agents?.defaults?.compaction?.memoryFlush;
78: 	if (defaults?.enabled === false) return null;
79: 	const softThresholdTokens = normalizeNonNegativeInt(defaults?.softThresholdTokens) ?? 4e3;
80: 	const forceFlushTranscriptBytes = parseNonNegativeByteSize(defaults?.forceFlushTranscriptBytes) ?? 2097152;
81: 	const reserveTokensFloor = normalizeNonNegativeInt(cfg?.agents?.defaults?.compaction?.reserveTokensFloor) ?? 2e4;
82: 	const { timeLine, userTimezone } = resolveCronStyleNow(cfg ?? {}, nowMs);
83: 	const dateStamp = formatDateStampInTimezone(nowMs, userTimezone);
84: 	const relativePath = `memory/${dateStamp}.md`;
85: 	const promptBase = ensureNoReplyHint(ensureMemoryFlushSafetyHints(defaults?.prompt?.trim() || DEFAULT_MEMORY_FLUSH_PROMPT));
86: 	const systemPrompt = ensureNoReplyHint(ensureMemoryFlushSafetyHints(defaults?.systemPrompt?.trim() || DEFAULT_MEMORY_FLUSH_SYSTEM_PROMPT));
87: 	return {
88: 		softThresholdTokens,
89: 		forceFlushTranscriptBytes,
90: 		reserveTokensFloor,
91: 		model: defaults?.model?.trim() || void 0,
92: 		prompt: appendCurrentTimeLine(promptBase.replaceAll("YYYY-MM-DD", dateStamp), timeLine),
93: 		systemPrompt: systemPrompt.replaceAll("YYYY-MM-DD", dateStamp),
---

FILE=/home/reckless-ideator/.openclaw/tools/node-v24.15.0/lib/node_modules/openclaw/dist/extensions/memory-core/index.js
HIT=81: 	const reserveTokensFloor = normalizeNonNegativeInt(cfg?.agents?.defaults?.compaction?.reserveTokensFloor) ?? 2e4;
67: function appendCurrentTimeLine(text, timeLine) {
68: 	const trimmed = text.trimEnd();
69: 	if (!trimmed) return timeLine;
70: 	if (trimmed.includes("Current time:")) return trimmed;
71: 	return `${trimmed}\n${timeLine}`;
72: }
73: function buildMemoryFlushPlan(params = {}) {
74: 	const resolved = params;
75: 	const nowMs = resolveMemoryCoreNowMs(resolved.nowMs);
76: 	const cfg = resolved.cfg;
77: 	const defaults = cfg?.agents?.defaults?.compaction?.memoryFlush;
78: 	if (defaults?.enabled === false) return null;
79: 	const softThresholdTokens = normalizeNonNegativeInt(defaults?.softThresholdTokens) ?? 4e3;
80: 	const forceFlushTranscriptBytes = parseNonNegativeByteSize(defaults?.forceFlushTranscriptBytes) ?? 2097152;
81: 	const reserveTokensFloor = normalizeNonNegativeInt(cfg?.agents?.defaults?.compaction?.reserveTokensFloor) ?? 2e4;
82: 	const { timeLine, userTimezone } = resolveCronStyleNow(cfg ?? {}, nowMs);
83: 	const dateStamp = formatDateStampInTimezone(nowMs, userTimezone);
84: 	const relativePath = `memory/${dateStamp}.md`;
85: 	const promptBase = ensureNoReplyHint(ensureMemoryFlushSafetyHints(defaults?.prompt?.trim() || DEFAULT_MEMORY_FLUSH_PROMPT));
86: 	const systemPrompt = ensureNoReplyHint(ensureMemoryFlushSafetyHints(defaults?.systemPrompt?.trim() || DEFAULT_MEMORY_FLUSH_SYSTEM_PROMPT));
87: 	return {
88: 		softThresholdTokens,
89: 		forceFlushTranscriptBytes,
90: 		reserveTokensFloor,
91: 		model: defaults?.model?.trim() || void 0,
92: 		prompt: appendCurrentTimeLine(promptBase.replaceAll("YYYY-MM-DD", dateStamp), timeLine),
93: 		systemPrompt: systemPrompt.replaceAll("YYYY-MM-DD", dateStamp),
94: 		relativePath
95: 	};
---
