# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 12:47 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1238_CDT.md`

## MATERIAL CHANGE

Installed OpenClaw 2026.7.1-2 source now proves the `cli_budget` compaction execution path.

`compactCliTranscript(params)`:
- receives `currentTokenCount`
- receives `contextTokenBudget`
- stamps runtime trigger `cli_budget`
- sets `promptTokenBudget = contextTokenBudget`
- invokes context-engine compaction with:
  - `force: true`
  - `compactionTarget: "budget"`
  - `tokenBudget: contextTokenBudget`
  - `currentTokenCount`
- applies `resolveCompactionTimeoutMs(params.cfg)`

Therefore `compactCliTranscript()` itself is not the decision point. Once invoked, it deliberately forces budget compaction.

The remaining root is upstream:

**WHAT CALLER DECIDES TO INVOKE `compactCliTranscript()`, AND HOW DOES IT COMPUTE `currentTokenCount` AND `contextTokenBudget`?**

The prior memory-core/context-window excavation is now demoted unless this upstream CLI-budget decision explicitly reuses those values.

## CURRENT EDGE

`MODEL → PAN → OPENCLAW EXEC → FORGE → RESULT → PAN = PASS`

Blocked:
`POST-TOOL CLI BUDGET DECISION → FORCED COMPACTION → TIMEOUT`

## NEXT

Read-only source proof:
1. locate all `compactCliTranscript(` call sites;
2. inspect caller conditions immediately before invocation;
3. identify construction of `currentTokenCount`;
4. identify construction of `contextTokenBudget`;
5. identify any ratio, reserve, minimum, threshold, or config key gating invocation;
6. capture exact v4 diagnostic start line if present, including `pre.estTokens`.

No mutation until the decision predicate is proven.

## PHASE

`CLI_BUDGET DECISION PREDICATE → NARROW REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
