# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 12:17 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1215_CDT.md`

## MATERIAL CHANGE

Wolfram was used to verify the compaction arithmetic under the currently suspected values.

Given:
- context window = 16384
- inherited/default reserve = 20000
- minimum prompt budget = min(8000, 0.5 * context) = 8000
- context-aware reserve clamp = 16384 - 8000 = 8384
- soft threshold candidate = 4000

Wolfram result:
- effective reserve after clamp = 8384
- remaining native prompt budget before projected turn = 8000
- if the generic embedded threshold uses that clamped reserve plus softThresholdTokens=4000:
  threshold = 16384 - 8384 - 4000 = 4000
- 4000 tokens is 24.4140625% of the 16384-token context

This is a strong quantitative explanation for premature threshold compaction, but remains conditional until the embedded preflight call chain proves the actual values supplied to `reserveTokensFloor` and `softThresholdTokens`.

Additional source evidence:
- Pan provider API is `openai-completions`.
- OpenAI Responses transport has a separate `responsesCompactThreshold` path.
- generic embedded preflight computes threshold independently with `contextWindow - reserveTokensFloor - softThresholdTokens`, bounded by a server threshold.

## NEXT

Resolve the values entering the generic embedded preflight:
- origin of `reserveTokensFloor`
- origin of `softThresholdTokens`
- whether either is context-adjusted before threshold calculation
- actual v4 token count at threshold trigger

Then choose the smallest validated configuration repair.

## PHASE

`EFFECTIVE EMBEDDED THRESHOLD PROOF → RESPONSE-LIFECYCLE REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
