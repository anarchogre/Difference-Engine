# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 12:27 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1225_CDT.md`

## MATERIAL CHANGE

Installed `memory-core` source now proves the default flush plan:

- if `agents.defaults.compaction.memoryFlush.enabled === false`, resolver returns `null`;
- otherwise `softThresholdTokens = 4000` by default;
- `reserveTokensFloor = agents.defaults.compaction.reserveTokensFloor ?? 20000`;
- therefore absent explicit overrides, memory-core returns a live plan with reserve=20000 and soft=4000.

Wolfram verified:
`max(contextWindow - 20000 - 4000, 0) > 0`
iff:
`contextWindow > 24000`.

Thus, if the embedded preflight truly used Pan's declared 16384-token context, the memory-core branch could not have generated the observed threshold event.

Given the server-threshold branch has already been ruled out for `pan-local/openai-completions` without explicit response-compaction enablement, the remaining contradiction points directly at context resolution:

**the embedded compaction preflight likely resolved a context window larger than 24000, or the observed threshold event originated from a different call path than the one currently modeled.**

## NEXT

Inspect `resolveMemoryFlushContextWindowTokens()` and the exact values passed to it in v4:
- model context source
- provider/model registry source
- fallback/default source
- any clamping/normalization
- whether 16384 from Pan provider config is actually honored by embedded preflight

No config mutation yet.

## PHASE

`CONTEXT-WINDOW RESOLUTION PROOF → RESPONSE-LIFECYCLE REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
