# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 12:29 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1227_CDT.md`

## MATERIAL CHANGE

Context-window resolution is now the only unresolved branch that can reconcile the observed v4 threshold compaction with the installed default memory-core plan.

Observed installed source:
- `resolveMemoryFlushContextWindowTokens()` delegates to `resolveContextTokensForModel(...)`.
- provider is resolved through `resolveFollowupContextConfigProvider(...)`.
- model id is taken from `params.followupRun.run.model ?? params.defaultModel`.
- memory-core defaults remain:
  - reserveTokensFloor = 20000
  - softThresholdTokens = 4000
- server response-compaction remains ruled out for `pan-local` / `openai-completions` absent explicit enablement.

Wolfram re-verified:
`max(c - 20000 - 4000, 0) > 0` iff `c > 24000`.

Therefore an observed positive threshold from the default memory-core plan requires the embedded preflight to resolve a context window greater than 24000 tokens.

## NEXT

Read-only proof:
1. locate and inspect `resolveContextTokensForModel`;
2. locate and inspect `resolveFollowupContextConfigProvider`;
3. identify precedence among agent cap, provider model context, registry metadata, session/trusted window, and fallback;
4. search v4 logs for the actual `preflightCompaction check` line and resolved contextWindow/threshold;
5. compare resolved runtime window to configured Pan model window = 16384.

No mutation until runtime/context source mismatch is proven.

## PHASE

`CONTEXT RESOLVER PRECEDENCE PROOF → RUNTIME WINDOW PROOF → RESPONSE-LIFECYCLE REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
