# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 13:43 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1341_CDT.md`

## MATERIAL CHANGE

Full-reference search located the actual mutation sites for llama-server prompt/cache/generation accounting.

In `tools/server/server-context.cpp`:

- line 3411:
  `slot.n_prompt_tokens_cache = n_past`
- line 3412:
  `slot.n_prompt_tokens_processed = 0`
- line 3497:
  `slot.n_prompt_tokens_processed += n_tokens_out`
- line 3536:
  `slot.n_prompt_tokens_processed++`
- line 3589:
  `slot.n_decoded = 0`
- line 3832:
  `slot.n_decoded += 1`
- line 3974:
  `slot.n_decoded += 1`

Wolfram confirms the accounting streams are structurally separated.

## INTERPRETATION

The source now proves that:
- cached prompt accounting is initialized from `n_past`;
- fresh prompt processing is reset independently and incremented during prompt work;
- generation accounting is reset independently and incremented later in decode paths.

The current evidence does not yet establish:
- what `n_past` equals for task 3142;
- what `n_tokens_out` represents in the bulk increment path;
- which generation increment path would have applied to task 3142;
- whether task 3142 ever crossed from prompt processing into generation before cancellation.

## NEXT

Inspect only the narrow source windows around:
- 3385–3550
- 3570–3605
- 3810–3990

Goal:
recover exact transition semantics from cache/LCP reuse → prompt evaluation → generation.

**Reality is final validator.**
