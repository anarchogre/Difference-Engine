# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 13:35 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1332_CDT.md`

## MATERIAL CHANGE

The compiled `llama-server` binary contains embedded absolute source paths.

Two distinct roots are now proven:

- installed binaries:
  `/home/reckless-ideator/.local/libexec/Difference-Engine/llama.cpp`
- source tree used to build them:
  `/home/reckless-ideator/.local/src/difference-engine/llama.cpp`

Wolfram confirms the roots are distinct.

The previous grep failed to expose semantics because it searched the binary-install root, not the source tree.

## NEXT

Search the actual source tree for the two exact llama-server log strings:

- `stop processing: n_tokens`
- `prompt processing, n_tokens`

Then inspect surrounding calculations and variable meanings before interpreting `n_tokens=7326` or changing timeout/prompt configuration.

**Reality is final validator.**
