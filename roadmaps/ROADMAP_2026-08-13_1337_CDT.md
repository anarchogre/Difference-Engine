# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 13:37 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1335_CDT.md`

## MATERIAL CHANGE

Installed llama.cpp source resolves the apparent token-counter contradiction.

Source:
`tools/server/server-context.cpp`

Prompt-processing progress log:
- printed `n_tokens` = `n_prompt_tokens_processed`
- `progress` = `prompt.n_tokens() / task->n_tokens()`
- reported rate = `n_prompt_tokens_processed / t_prompt_processing`

Release log:
- printed `n_tokens` = `prompt.n_tokens()`

Therefore the two observed `n_tokens` values are not the same metric.

For task 3142:
- checkpoint `n_tokens = 2048` means `n_prompt_tokens_processed = 2048`
- checkpoint `progress = 0.78` is based on `prompt.n_tokens() / task->n_tokens()`
- release `n_tokens = 7326` means `prompt.n_tokens() = 7326` at release

The earlier 72.91 tokens/s extrapolation is invalid and must not be retained as an interpretation.

Wolfram confirms:
`CheckpointPrintedMetric != ReleasePrintedMetric`

## INTERPRETATION

The prompt-processing checkpoint and release counter belong to different state variables.

This removes the apparent 4.15x rate discontinuity without requiring any inference about sudden acceleration.

Still unresolved:
- how `n_prompt_tokens_processed` is incremented;
- how `prompt.n_tokens()` changes during LCP/cache reuse and prompt evaluation;
- exact `task->n_tokens()` for task 3142;
- whether generation ever began before cancellation.

## NEXT

Inspect source updates for:
- `n_prompt_tokens_processed`
- `prompt.n_tokens()`
- `task->n_tokens()`

The next goal is to determine how much of the 180-second budget was spent on fresh prompt evaluation versus retained/cached context accounting.

**Molasses removes impossibilities. Reality is final validator.**
