# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 13:49 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1348_CDT.md`

## MATERIAL CHANGE

The acceptance JSON grep did not recover the original v6 launcher invocation.

Observed fields:
- `status = timeout`
- `timeoutPhase = provider`
- timeout message references `agents.defaults.timeoutSeconds`
- sessionId and sessionKey are present

Not observed:
- actual CLI argv
- actual launcher script line
- explicit `--timeout 180` invocation

Wolfram confirms that the invocation remains unrecovered from the inspected JSON fields.

## INTERPRETATION

Do not reconstruct v7 from memory while the exact v6 launcher is recoverable from evidence.

The next operation is to enumerate the v6 evidence directory and identify the launcher/script/command artifact that created `acceptance.json`.

## NEXT

List only the top-level filenames in the v6 evidence directory.

**Reality is final validator.**
