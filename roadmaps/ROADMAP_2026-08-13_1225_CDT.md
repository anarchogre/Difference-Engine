# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 12:25 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1223_CDT.md`

## MATERIAL CHANGE

Threshold-source discrimination is now effectively resolved.

Observed:
- Pan provider is `pan-local`, API `openai-completions`.
- Installed source requires non-`openai` providers to set `responsesServerCompaction === true` before `resolveResponsesServerCompactionThreshold()` can return a positive server threshold.
- The discriminator probe produced no `responsesServerCompaction` or `responsesCompactThreshold` configuration hits.
- Bundled `memory-core` explicitly registers:
  `flushPlanResolver: buildMemoryFlushPlan`.

Wolfram formalization confirms:
- under non-`openai` provider + no explicit server-compaction enable, server-threshold branch is false;
- with a registered memory flush resolver, the memory-plan branch remains possible.

Therefore the controlling threshold branch is now:

**MEMORY-CORE FLUSH PLAN**

This is evidence-based but still requires inspection of `buildMemoryFlushPlan` before mutation.

## NEXT

Read-only inspect `buildMemoryFlushPlan`:
- exact enable/disable behavior
- returned `reserveTokensFloor`
- returned `softThresholdTokens`
- interaction with 16384-token Pan context
- any config keys controlling the plan

Then compute the actual threshold and make the smallest validated repair.

## PHASE

`MEMORY-CORE FLUSH PLAN PROOF → RESPONSE-LIFECYCLE REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
