# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 12:23 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1221_CDT.md`

## MATERIAL CHANGE

Threshold-source diagnosis narrowed further.

Installed OpenClaw source proves:
- `resolveMemoryFlushPlan()` is plugin-driven:
  `memoryPluginState.capability?.capability.flushPlanResolver?.(params) ?? null`
- absence of a persisted `agents.defaults.compaction.memoryFlush` config path does NOT prove that no memory flush plan exists.
- `resolveResponsesServerCompactionThreshold()` requires:
  - provider/model identifiers,
  - and for providers other than literal `openai`, `responsesServerCompaction === true`.
- Pan is configured through provider `pan-local` with API `openai-completions`.

Therefore the remaining discriminator is concrete:
1. determine whether the active memory-core capability registered a flush-plan resolver and what values it returns;
2. determine whether Pan's model/provider configuration explicitly supplies `responsesServerCompaction` / `responsesCompactThreshold`.

## NEXT

Read-only branch proof:
- inspect registrations/usages of `flushPlanResolver`
- inspect memory-core resolver implementation
- inspect Pan config only for response-compaction keys
- inspect persisted OpenClaw config for those keys

Then identify the actual positive-threshold branch and repair only that branch.

## PHASE

`MEMORY-PLAN VS SERVER-THRESHOLD DISCRIMINATION → RESPONSE-LIFECYCLE REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
