# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 12:15 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1210_CDT.md`

## MATERIAL CHANGE

Installed OpenClaw 2026.7.1-2 source now confirms two distinct compaction-control paths are present:

1. OpenAI Responses transport:
   - `responsesCompactThreshold`
   - transport resolves `compactThreshold` from `options.extraParams?.responsesCompactThreshold` or model defaults.

2. Embedded-agent/session compaction:
   - threshold arithmetic includes:
     `contextWindow - reserveTokens - softThreshold`
   - a `minimumThresholdTokens` input is supplied from `serverCompactionThreshold`.

Additional installed constants:
- native-thread default reserve tokens: 20000
- minimum prompt budget tokens: 8000
- minimum prompt budget ratio: 0.5

For a 16384-token model, context-aware clamping can reduce the inherited 20000-token reserve to at most 8384 tokens in the native-thread path, leaving an 8000-token prompt budget before projected-turn subtraction.

This arithmetic is now a strong explanation candidate for very early compaction, but the exact v4 trigger path is not yet proven because Pan is configured through `openai-completions`, while one discovered threshold source belongs to the OpenAI Responses transport.

## NEXT

Resolve the exact call chain around:
- `resolvePositiveIntegerParam(..., "responsesCompactThreshold")`
- construction of `serverCompactionThreshold`
- `minimumThresholdTokens: serverCompactionThreshold`
- generic embedded-run threshold state
- whether these paths execute for `pan-local` / `openai-completions`

Then capture the actual v4 token state immediately before threshold compaction.

No config mutation until that path is proven.

## PHASE

`COMPACTION CALL-CHAIN PROOF → EFFECTIVE V4 THRESHOLD → RESPONSE-LIFECYCLE REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
