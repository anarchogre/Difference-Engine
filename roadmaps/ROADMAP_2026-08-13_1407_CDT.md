# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 14:07 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1406_CDT.md`

## MATERIAL CHANGE

The existing managed OpenClaw installation is now resolved by absolute path:

- CLI shim:
  `/home/reckless-ideator/.openclaw/tools/node-v24.15.0/bin/openclaw`
- package root:
  `/home/reckless-ideator/.openclaw/tools/node-v24.15.0/lib/node_modules/openclaw`
- module entrypoint:
  `/home/reckless-ideator/.openclaw/tools/node-v24.15.0/lib/node_modules/openclaw/openclaw.mjs`

The first v7 attempt remains classified:
`LAUNCHER_FAILURE_BEFORE_OPENCLAW`

Its 84-byte capture must be preserved before rerunning the controlled acceptance.

## CONTROLLED RETRY

Retry v7 with:
- same session key;
- same model;
- same acceptance prompt;
- same git command;
- same JSON mode;
- same 600-second outer timeout;
- absolute managed OpenClaw CLI path.

Only launcher resolution changes relative to the failed pre-OpenClaw attempt. Relative to v6's actual OpenClaw run, the intended test delta remains:
`--timeout 180 -> --timeout 600`.

Preserve the failed launcher capture as:
`launcher-failure-before-openclaw.txt`

## NEXT

Run the absolute-path v7 acceptance.

**Reality is final validator.**
