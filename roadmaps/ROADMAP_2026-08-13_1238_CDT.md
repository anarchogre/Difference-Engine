# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 12:38 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1235_CDT.md`

## MATERIAL CORRECTION

New runtime evidence changes the leading diagnosis.

Observed in v4:
- every model transport request uses:
  - provider = `pan-local`
  - api = `openai-completions`
  - model = `pan-qwen`
- configured provider model entry exists:
  - id = `pan-qwen`
  - contextWindow = 16384
- installed context resolver explicitly checks configured provider/model context.
- final v4 compaction diagnostic says:
  `trigger=cli_budget`

Separately, the visible compaction lifecycle event says:
`reason=threshold`.

Wolfram verification under the already-proven memory-core defaults and 16384 context:
- memory preflight threshold = 0
- memory preflight cannot trigger

Therefore the strongest current interpretation is:

**WE WERE TRACING THE WRONG COMPACTION SUBSYSTEM.**

The `reason=threshold` lifecycle event is likely emitted by a compaction routine invoked from the separate `cli_budget` trigger path, rather than proving that memory-core preflight thresholding initiated the compaction.

This demotes the model-window lookup-miss hypothesis.

## NEXT

Trace only the installed `cli_budget` path:
1. find all `cli_budget` literals;
2. find `[compaction-diag]` construction;
3. identify the function that decides CLI-budget compaction;
4. identify its token budget and threshold inputs;
5. identify its config/control surface;
6. repair that path only.

Do not mutate memory-core or context-window configuration unless the CLI-budget trace points back to them.

## CURRENT EDGE

Actuator primitive:
`MODEL → PAN → OPENCLAW EXEC → FORGE → RESULT → PAN = PASS`

Remaining:
`PAN POST-TOOL RESPONSE → CLI-BUDGET COMPACTION → CLEAN TERMINATION = BLOCKED`

## PHASE

`CLI_BUDGET ROOT TRACE → RESPONSE-LIFECYCLE REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
