# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 13:39 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1337_CDT.md`

## MATERIAL CHANGE

Installed llama.cpp source further resolves token-state semantics in `tools/server/server-context.cpp`.

Proven:
- `n_prompt_tokens_processed` is a dedicated prompt-evaluation counter.
- `n_prompt_tokens_cache` is tracked separately.
- `prompt.n_tokens()` is the current slot prompt/token-state size.
- progress logging uses:
  `prompt.n_tokens() / task->n_tokens()`
- release logging uses:
  `prompt.n_tokens()`
- sampled/generated tokens are appended to the same prompt token container:
  `prompt.tokens.push_back(sampled)`
- generation capacity calculations also use `prompt.n_tokens()`.

Therefore:
- checkpoint `2048` is fresh prompt-evaluation accounting;
- checkpoint `0.78` is not `2048 / total_prompt_tokens`;
- release `7326` is current slot token-state size at release;
- release `7326` may include cached/retained prompt state, later prompt evaluation, and—if generation began—generated tokens.

The checkpoint cannot be converted to total task prompt size without either:
- checkpoint-time `prompt.n_tokens()`, or
- `task->n_tokens()`.

Wolfram confirms only the conditional relation:
`task_tokens = checkpoint_prompt_tokens / 0.78`.

The earlier use of 2048 as the progress numerator is invalid and retired.

## NEXT

Inspect exact assignments/increments for:
- `n_prompt_tokens_processed`
- `n_prompt_tokens_cache`
- `n_decoded`

Goal:
separate cached/retained prompt state, fresh prompt evaluation, and generated tokens for task 3142 before changing runtime budgets.

**Molasses removes impossibilities. Reality is final validator.**
