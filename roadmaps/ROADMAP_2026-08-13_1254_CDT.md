# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 12:54 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1251_CDT.md`

## MATERIAL CHANGE

Installed OpenClaw source now exposes the inner CLI preemptive-compaction construction far enough to formalize the actual decision.

Observed in `attempt.tool-run-context-CuQsIXnb.js`:

- `promptBudgetBeforeReserve = max(1, contextTokenBudget - effectiveReserveTokens)`
- `overflowTokens = max(0, estimatedPromptTokens - promptBudgetBeforeReserve)`
- `shouldCompact = route === "compact_only" || route === "compact_then_truncate"`
- the source emits a diagnostic containing:
  - route
  - estimatedPromptTokens
  - pressureSource
  - promptBudgetBeforeReserve
  - overflowTokens
  - toolResultReducibleChars
  - reserveTokens
  - effectiveReserveTokens
  - contextTokenBudget
  - message counts
  - session file

Outer CLI lifecycle already proved:

`currentTokenCount = max(estimatedPromptTokens, freshTokenSnapshot)`

and compaction occurs unless:

`shouldCompact == false AND currentTokenCount <= promptBudgetBeforeReserve`.

Wolfram formalization:

`PROMPT_BUDGET = max(1, B - R_eff)`

`CURRENT = max(E, S)`

If route already selects compaction, trigger is unconditional.
Otherwise trigger iff:

`max(E, S) > max(1, B - R_eff)`.

## REMAINING UNKNOWN

No mutation yet. We still need the actual v4 values:

- route
- estimatedPromptTokens
- token snapshot
- pressure source
- reserveTokens
- effectiveReserveTokens
- contextTokenBudget
- promptBudgetBeforeReserve

The source already provides a diagnostic format for nearly all of these, so runtime logs should be queried before more static excavation.

## NEXT

1. Search v4 trace/session logs for `[context-overflow-precheck]`.
2. If present, capture its numeric values.
3. Independently capture the v4 session entry fields:
   - contextTokens
   - totalTokens
   - totalTokensFresh
4. Only if runtime diagnostics are absent, inspect source lines ~190-260 for `effectiveReserveTokens` and route selection.
5. Compute the exact v4 trigger numerically in Wolfram.
6. Repair only the proven input/configuration defect.

## CURRENT EDGE

`MODEL → PAN → OPENCLAW EXEC → FORGE → RESULT → PAN = PASS`

Blocked:

`PREEMPTIVE CLI BUDGET VALUES → FORCED COMPACTION → TIMEOUT`

## PHASE

`RUNTIME PRECHECK VALUE PROOF → NUMERIC ROOT CAUSE → NARROW REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
