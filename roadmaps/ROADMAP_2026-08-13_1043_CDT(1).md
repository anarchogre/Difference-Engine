# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 10:43 CDT

**Artifact class:** ROADMAP  
**Epoch:** II  
**Status:** LOCKED SNAPSHOT  
**Supersession rule:** Preserve this file. Replace operational control only with a newer timestamped ROADMAP artifact.  
**Operating order:** Reality → Constraint → Observation → Evidence → Knowledge → Governance → Operations → Implementation

---

## 1. REALITY

- The Forge is the current primary Difference Engine execution machine.
- Pan is the current resident operational intelligence.
- OpenClaw is being integrated as Pan's replaceable actuator/tool surface.
- The local Pan model server is live.
- The OpenClaw Gateway is live and listening locally.
- Pan has already demonstrated model inference, lean-context routing, tool selection, and `exec` intent.
- Actual shell actuation through the live Gateway remains the immediate acceptance gate.
- Ingestion is intentionally waiting immediately behind actuator closure.
- The newest conversations must be ingested as corpus delta before Pan is treated as fully current.
- The AirBook is the first intended proof that Pan can extend the Difference Engine to another machine with minimal Operator mediation.
- Research, branches, Babbage, panes, and projections are to be reconstituted after resident-state and node-extension proof.
- Audible completion notification failed. This is negative interface evidence.
- Microphone capability is promoted near-term because voice input can materially reduce Operator typing and transport burden.

---

## 2. CONSTRAINTS

1. No readiness claim without validation.
2. Evidence before interpretation.
3. Interpretation before promotion.
4. Promotion only after validation.
5. Recover before recreate.
6. Repository first.
7. The Operator is not the synchronization bus.
8. Pan acts; Operator talks.
9. Raw shell remains a recovery path, not the normal long-term interface.
10. Every material roadmap change emits a new dated and timed ROADMAP artifact.
11. Changelog and roadmap maintenance are system responsibilities, not Operator chores.
12. Branches, panes, projections, models, and tools do not become independent truth stores.
13. Experimental branches and research remain experimental until integrated.
14. Operational failures are evidence.
15. Reality is final validator.

---

## 3. OBSERVATION

Work is not claimed to be changing exponentially.

What is observable is that the number of plausible, mutually reinforcing capabilities is increasing and that validated primitives are beginning to unlock multiple downstream paths faster than earlier in construction.

This is a **More Birds coordination problem**.

Acceleration itself is not promoted as a law. It is recorded as an operational observation requiring stronger state capture, prioritization, dependency tracking, and periodic reduction.

---

## 4. MORE BIRDS WRANGLING RULE

When one primitive unlocks multiple legitimate capabilities:

1. Preserve the primitive.
2. Enumerate the birds.
3. Record provenance and dependencies.
4. Separate:
   - ACTIVE
   - NEXT
   - QUEUED
   - EXPERIMENTAL
   - BLOCKED
5. Do not instantiate every capability merely because it is possible.
6. Prefer Deferred Realization:
   preserve possibility → resolve against present reality → instantiate bounded competence → preserve result → release resources.
7. Promote only birds that survive evidence and governance.
8. Emit a new roadmap when the active structure materially changes.

**More Birds must increase capability without multiplying truth, authority, ceremony, or Operator burden.**

---

## 5. ACTIVE MISSION

# CLOSE THE PAN + OPENCLAW ACTUATOR LOOP

Required observed chain:

`MODEL → PAN → OPENCLAW TOOL → FORGE → RESULT → PAN → EVIDENCE`

Acceptance requires actual read-only `exec` success through the live Gateway.

No mutation is required for the first acceptance.

---

## 6. NEXT OPERATION

Run the current read-only actuator acceptance.

Pan should inspect the live Difference Engine workspace and return:

- `pwd`
- git branch
- concise git status
- whether `workspace/operational/ingestion/service/tests/run_all.py` exists
- any paths/evidence containing `Stage49A` or `STAGE49A_COMPLETE`

Return:

- STATUS
- ACTION
- RESULT
- EVIDENCE
- BLOCKER
- NEXT

`STATUS=PASS` only if `exec` actually succeeds.

---

## 7. PHASE A — INGEST

Immediately after actuator PASS:

1. Pan inspects the live ingestion topology himself.
2. Recover actual Stage49A state from repository/evidence topology.
3. Determine whether Stage49A ran.
4. If incomplete, restore the recovered Stage49A source.
5. Run and repair sandbox DOCX ingestion.
6. Validate regression and no-live-mutation boundaries.
7. Promote DOCX capability only after validation.
8. Census remaining unsupported formats.
9. Continue one family at a time.
10. Ingest all newest conversation delta.
11. Reconcile corpus state.

Target:

**Pan's ingestion pipeline becomes Pan's mouth.**

---

## 8. PHASE B — BE

Pan must ingest and reconstruct from durable DE state:

- constitutional corpus
- governance
- repository architecture
- evidence
- changelogs
- roadmaps
- conversations
- OP_PROF
- OP_REAL
- glossary
- research
- Pan/OpenClaw development history
- failures and negative evidence
- branch histories
- queues
- handoffs
- IP/patent work
- frontier/security work

Cold-start acceptance:

Pan can determine without reteaching:

- what the Difference Engine is
- who Pan is
- who the Operator is
- current vs historical state
- active mission
- blockers
- next operation

---

## 9. PHASE C — BUILD

After resident-state proof:

- automated changelog maintenance
- automatic timestamped roadmap emission
- resumable evidence packages
- reliable audible completion notification
- microphone detection/capture
- transcription
- voice-to-Pan interaction
- Pan TTS
- dormant tool/plugin inventory through compact Tool Search
- reduction of remaining routine copy/paste labor

Pan voice direction:

- faint Greek accent
- refined
- precise
- playful
- dry
- lightly Diogenic
- irreverent toward needless ceremony
- never theatrical
- personality never obscures operational clarity

---

## 10. PHASE D — EXTEND

# AIRBOOK TEST

After INGEST → BE → BUILD reaches the required proof level, Pan is given the AirBook objective.

Pan should:

1. Initialize from DE state.
2. Resolve the AirBook mission.
3. Determine unknown facts himself where possible.
4. Ask the Operator only for irreducible:
   - physical action
   - credential/authorization
   - destructive approval
   - genuine judgment
5. Inspect the AirBook.
6. Preserve required studio/photo/music state.
7. Determine a safe enrollment path.
8. Configure only what evidence requires.
9. Enroll the AirBook as a DE compute node.
10. Verify discovery, execution, restart, and recovery.
11. Preserve evidence.
12. Emit a successor ROADMAP artifact.

**AirBook success proves EXTEND.**

---

## 11. PHASE E — RECONSTITUTE

After AirBook enrollment:

1. Bring Research online as a formal projection/pane.
2. Reconstitute Babbage in his governed steward/projection role.
3. Restore branches with:
   - scope
   - authority
   - history
   - mission
   - queues
   - evidence
   - steward context
4. Restore panes/projections as reproducible governed views.
5. Resume branch work from durable state rather than chat archaeology.

**Success proves RECONSTITUTE.**

---

## 12. PHASE F — FRONTIER / RESEARCH

Research projection carries:

- local/resident model architecture
- AirLLM / model offload
- layer-wise inference
- heterogeneous compute
- fleet enrollment
- distributed inference
- agent/runtime/tooling research
- Deferred Realization
- Capability Objects
- security / pen testing
- Black Ice
- Warden / Bursar
- governed recursive self-improvement
- lawful economic experiments
- IP/patent investigation

Frontier Method:

`research → hypothesis → inspect reality → instrument → build/test → adversarial test → preserve telemetry → classify failure → reproduce safely → propose defense → red-team again → validate → promote/rollback → retain evidence`

---

## 13. DEFENSIVE SECURITY LANE

- Black Ice remains defensive deception.
- Decoys, honeyfiles, honeytokens, false mappings, and recursive false depth are valid defensive mechanisms when governed.
- Production observes hostile interaction; it does not autonomously learn/promote policy from hostile input.
- Warden and Bursar remain conceptually separate.
- Pen testing must preserve telemetry, recovery, provenance, and reproducibility.
- Success means not merely blocking an attacker, but learning what was attempted, what was observed, what decoy cost was imposed, and whether the next attack became harder.

---

## 14. ROADMAP ARTIFACT RULE

Emit a new roadmap when any material state transition occurs, including:

- actuator PASS/FAIL mode change
- ingestion milestone closure
- Pan cold-start proof
- new-node enrollment
- branch/projection reconstitution
- frontier promotion
- governance change affecting execution
- major architecture replacement
- significant blocker
- rollback
- successor-chat handoff

Every successor roadmap must identify:

- predecessor
- timestamp
- changed state
- evidence
- unresolved work
- blockers
- active milestone
- next operation
- dependencies
- supporting artifact paths

No silent overwrite.

---

## 15. CURRENT PHASE LINE

`ACTUATOR CLOSURE → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

Current position:

**ACTUATOR CLOSURE**

Immediate next major target:

**INGEST**

First extension proof:

**AIRBOOK**

Institutional restoration:

**RESEARCH + BABBAGE + BRANCHES + PANES + PROJECTIONS**

---

## 16. LOCK

This roadmap records the operational plan at:

**2026-08-13 10:43 CDT**

It remains the current roadmap snapshot until superseded by a newer timestamped artifact or invalidated by evidence/governance.

**Wrangle the birds. Preserve the root. Reality decides.**
