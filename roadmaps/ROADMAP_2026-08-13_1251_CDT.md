# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 12:51 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1249_CDT.md`

## MATERIAL CHANGE

Installed OpenClaw source now exposes the actual CLI compaction trigger predicate.

Within `runCliTurnCompactionLifecycle(params)`:

- `contextTokenBudget = positive(sessionEntry.contextTokens)`
- if no session file or no positive contextTokenBudget: return
- a preemptive compaction state is computed
- `tokenSnapshot = fresh sessionEntry.totalTokens` when available
- `currentTokenCount = max(preemptiveCompaction.estimatedPromptTokens, tokenSnapshot ?? 0)`
- the lifecycle returns without compaction only when:
  - `preemptiveCompaction.shouldCompact == false`
  - AND `currentTokenCount <= preemptiveCompaction.promptBudgetBeforeReserve`

Wolfram formally simplified the complement of that return condition to:

`COMPACT_TRIGGER = preemptiveCompaction.shouldCompact OR currentTokenCount > preemptiveCompaction.promptBudgetBeforeReserve`

where:

`currentTokenCount = max(estimatedPromptTokens, tokenSnapshot)`

Therefore the root is no longer ambiguous at the outer lifecycle level.

## REMAINING UNKNOWN

The decision is controlled entirely by the construction of `preemptiveCompaction`:

- `shouldCompact`
- `estimatedPromptTokens`
- `promptBudgetBeforeReserve`

and by the fresh token snapshot when present.

These values are not yet proven.

## NEXT

Read-only inspect the exact function/expression that constructs `preemptiveCompaction` and the settings used to derive:
- reserve tokens
- prompt budget before reserve
- estimated prompt tokens
- shouldCompact

Then calculate v4's actual trigger numerically and repair only the proven input/configuration causing false or premature compaction.

## CURRENT EDGE

`MODEL → PAN → OPENCLAW EXEC → FORGE → RESULT → PAN = PASS`

Blocked:
`PREEMPTIVE CLI BUDGET PREDICATE → FORCED COMPACTION → TIMEOUT`

## PHASE

`PREEMPTIVE COMPACTION VALUE PROOF → NARROW REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
