# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 12:05 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1156_CDT.md`

## MATERIAL CHANGE

Installed OpenClaw 2026.7.1-2 source exposed additional compaction-control primitives.

Observed in installed runtime/type declarations:
- `softThresholdTokens`
- `reserveTokensFloor`
- `responsesCompactThreshold`
- threshold calculation:
  `contextWindow - reserveTokens - softThreshold`
- compaction threshold is inactive when the resolved threshold is <= 0
- runtime checks session token state against the computed threshold before compacting

`agents.defaults.compaction.reserveTokensFloor` is explicitly referenced by installed runtime.

`responsesCompactThreshold` is now a candidate control surface, but its exact configuration path and semantics are not yet established.

## GOVERNANCE

No config mutation is promoted from this discovery alone.

Next operation is read-only source/schema resolution of:
- exact configuration path for `responsesCompactThreshold`
- exact schema object containing `reserveTokensFloor`
- accepted sibling keys
- whether proactive response compaction can be disabled or pushed beyond the current 16K Pan context without disabling overflow recovery

## CURRENT PHASE

`INSTALLED COMPACTION CONTROL RESOLUTION → ACTUATOR RESPONSE-LIFECYCLE REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
