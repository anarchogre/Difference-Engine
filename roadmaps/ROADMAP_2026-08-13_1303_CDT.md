# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 13:03 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1301_CDT.md`

## MATERIAL CHANGE

Installed OpenClaw source now proves the primary embedded auto-compaction predicate exactly:

```text
if (!settings.enabled) return false;
return contextTokens > contextWindow - settings.reserveTokens;
```

The session call site proves:
- `contextWindow = this.model?.contextWindow ?? 0`
- the context token count is derived from either:
  - estimated branch context,
  - current context usage,
  - or `calculateContextTokens(assistantMessage.usage)`
- if the predicate is true:
  `runAutoCompaction("threshold", false)` is invoked.

This is the primary event that matches the observed v4 embedded lifecycle:
`reason=threshold`.

## WOLFRAM REDUCTION

Let:
- `T = contextTokens`
- `W = contextWindow`
- `R = settings.reserveTokens`

Then:

`TRIGGER = T > W - R`

equivalently:

`R + T > W`.

If `R > W`, then for every nonnegative `T`, the trigger is true.

For the specific candidate:
- `W = 16384`
- `R = 20000`

the threshold is:

`W - R = -3616`

so every nonnegative context-token count triggers compaction.

This is a conditional result only. The exact `settings.reserveTokens` used by this core session compactor is not yet proven and must not be inferred from memory-core's separate 20000-token reserve.

## NEXT

Read-only proof of the remaining decisive value:

1. locate `DEFAULT_COMPACTION_SETTINGS` definition;
2. locate settings-manager initialization / overrides for compaction;
3. capture the actual effective `settings.reserveTokens` for v4 if serialized or logged;
4. compare to `contextWindow = 16384`.

If core reserve is greater than or equal to the model window, the root cause is numerically proven.

## CURRENT EDGE

Proven:
`MODEL → PAN → EXEC → FORGE → RESULT → PAN FINAL TEXT = PASS`

Blocked:
`CORE shouldCompact(T,W,R) → runAutoCompaction("threshold") → TIMEOUT`

## PHASE

`CORE RESERVE VALUE PROOF → NUMERIC ROOT CAUSE → NARROW REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
