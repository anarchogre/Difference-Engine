# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 12:08 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1205_CDT.md`

## MATERIAL CHANGE

Installed OpenClaw 2026.7.1-2 source confirms:
- `agents.defaults.compaction.reserveTokensFloor` is a real accepted configuration path.
- `softThresholdTokens` exists in the installed type/schema surface.
- runtime defaults observed include `softThresholdTokens = 4000`.
- runtime compaction reserve floor observed defaults to `20000` before context-aware clamping/normalization.
- `responsesCompactThreshold` is resolved through a separate positive-integer parameter source and is not yet proven to be under `agents.defaults.compaction`.

No configuration change is promoted yet.

## NEXT

Resolve exact parent schema/config paths for:
- `softThresholdTokens`
- accepted siblings of `agents.defaults.compaction.reserveTokensFloor`
- `responsesCompactThreshold`
- context-aware reserve clamping for the 16384-token Pan model

Then choose the smallest deterministic repair for post-tool response completion.

## PHASE

`INSTALLED COMPACTION PATH RESOLUTION → ACTUATOR RESPONSE-LIFECYCLE REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
