# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 12:10 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1209_CDT.md`

## MATERIAL CHANGE

Installed OpenClaw 2026.7.1-2 source now exposes a likely context-size mismatch:

- `agents.defaults.compaction.reserveTokensFloor` is real.
- code paths default the reserve floor to 20000 tokens when unset.
- Pan's configured model context is 16384 tokens.
- installed code contains context-aware clamping of reserve tokens against the model context and minimum prompt budget.
- therefore the default reserve floor is larger than Pan's entire configured context and must be treated as a prime suspect for premature compaction.
- this is not yet proven to be the exact trigger for the v4/v5 run.

The latest source trace did not yet establish the exact parent/source for `responsesCompactThreshold`.

## NEXT

Read-only, narrow source/runtime proof:

1. resolve every occurrence and caller of `responsesCompactThreshold`;
2. inspect the exact threshold-state construction and call site;
3. capture Pan's current compaction config;
4. capture v4 session token/compaction diagnostics;
5. compute the effective threshold from observed runtime values.

Only after that should `reserveTokensFloor` or any other compaction key be changed.

## PHASE

`COMPACTION ARITHMETIC PROOF → RESPONSE-LIFECYCLE REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
