# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 13:05 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1303_CDT.md`

## MATERIAL CHANGE

Installed OpenClaw 2026.7.1-2 source now proves the core compaction defaults and getter fallback:

`DEFAULT_COMPACTION_SETTINGS = { enabled: true, reserveTokens: 16384, keepRecentTokens: 20000 }`

`getCompactionReserveTokens() = this.settings.compaction?.reserveTokens ?? 16384`

Primary embedded threshold predicate already proved:

`shouldCompact(contextTokens, contextWindow, settings) = contextTokens > contextWindow - settings.reserveTokens`

For Pan v4:

`contextWindow = 16384`

Therefore, absent an explicit runtime override of the core compaction reserve:

`threshold = 16384 - 16384 = 0`

and:

`shouldCompact = contextTokens > 0`

Wolfram formally verifies:
- threshold = 0
- zero tokens do not trigger
- every positive token count triggers
- `keepRecentTokens = 20000` exceeds the 16384-token window by 3616

This exactly matches the observed primary embedded event:
`runAutoCompaction("threshold", false)`

after a successful tool result and generated `STATUS=PASS`.

## REMAINING VALIDATION

Before mutation, prove no effective override exists in `this.settings.compaction.reserveTokens` for the v4 agent/session.

The trajectory config snapshot shows no `agents.defaults.compaction` stanza, but the core SettingsManager may source settings separately. Do not conflate config layers.

## NEXT

Read-only:
1. inspect effective core SettingsManager compaction settings at runtime or persisted settings source;
2. confirm effective reserve = 16384 and enabled = true;
3. preserve current settings;
4. apply the smallest valid repair that makes reserve < context window;
5. rerun fresh actuator acceptance;
6. require clean end-to-end return, not merely generated assistant text.

## CURRENT EDGE

Proven:
`MODEL → PAN → EXEC → FORGE → RESULT → PAN FINAL TEXT = PASS`

Blocked:
`CORE THRESHOLD (candidate threshold 0) → AUTO-COMPACTION → TIMEOUT`

## PHASE

`EFFECTIVE CORE RESERVE CONFIRMATION → NARROW REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
