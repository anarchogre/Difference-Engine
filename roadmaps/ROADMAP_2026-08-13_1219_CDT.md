# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 12:19 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1217_CDT.md`

## MATERIAL CHANGE

Wolfram verified the generic embedded preflight arithmetic from installed OpenClaw source.

For Pan contextWindow = 16384:

- no memoryFlushPlan/config reserve override:
  reserveTokensFloor = 20000
- no memoryFlushPlan:
  softThresholdTokens = 4000
- no Responses server threshold:
  generic threshold = max(16384 - 20000 - 4000, 0) = 0

The helper returns no threshold state when threshold <= 0.

Therefore the raw 20000/4000 defaults alone CANNOT explain the observed v4 `reason=threshold` compaction.

At least one of the following must be true in the actual v4 path:
1. `memoryFlushPlan` existed and supplied a smaller reserve and/or soft threshold producing a positive threshold;
2. `serverCompactionThreshold` was positive;
3. another threshold-producing path fed the observed compaction event.

This corrects the prior hypothesis that default reserve arithmetic alone caused premature compaction.

## NEXT

Resolve:
- `resolveMemoryFlushPlan`
- `resolveResponsesServerCompactionThreshold`
- whether `openai-completions` can yield a positive server compaction threshold
- actual v4 preflight threshold log line / token count if present

No config mutation until one of these paths is proven.

## PHASE

`ACTUAL THRESHOLD SOURCE PROOF → RESPONSE-LIFECYCLE REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
