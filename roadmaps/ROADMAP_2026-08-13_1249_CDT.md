# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 12:49 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1247_CDT.md`

## MATERIAL CHANGE

Installed OpenClaw 2026.7.1-2 source now proves:

- `compactCliTranscript()` has one visible call site in `cli-compaction-CF7Yb1P6.js` near line 384.
- it receives `currentTokenCount` and `contextTokenBudget` from its caller.
- once invoked, it forces budget compaction:
  - `trigger = "cli_budget"`
  - `force = true`
  - `compactionTarget = "budget"`
- the v4 diagnostic confirms the same `cli_budget` path and timeout.
- helper code near the same file shows fresh persisted `sessionEntry.totalTokens` can be used as a token-count source.
- `useContextEngineCompaction` gates the call to `compactCliTranscript()`, but its own derivation and the derivation of `contextTokenBudget` / `currentTokenCount` are not yet captured.

Wolfram supplied no OpenClaw-specific external facts; installed source remains controlling evidence.

## NEXT

Read only the exact local source windows needed to close the predicate:

1. helper definitions around lines 40-110;
2. caller logic around lines 300-425;
3. identify:
   - function name owning the call;
   - `currentTokenCount` derivation;
   - `contextTokenBudget` derivation;
   - `useContextEngineCompaction` derivation;
   - all early-return / threshold / guard conditions before line 384.

No config mutation yet.

## CURRENT EDGE

`MODEL → PAN → OPENCLAW EXEC → FORGE → RESULT → PAN = PASS`

Blocked:
`CLI_BUDGET CALLER PREDICATE → FORCED COMPACTION → TIMEOUT`

## PHASE

`CLI_BUDGET CALLER PREDICATE → NARROW REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
