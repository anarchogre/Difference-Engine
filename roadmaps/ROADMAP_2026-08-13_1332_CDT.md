# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 13:32 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1330_CDT.md`

## MATERIAL CHANGE

The complete task-3142 log slice contains only four lifecycle lines:

1. task launch at 13:14:12
2. prompt-processing checkpoint at 13:16:09:
   - n_tokens = 2048
   - progress = 0.78
   - elapsed = 116.61 s
   - rate = 17.56 tokens/s
3. cancellation at 13:17:12
4. slot release at 13:17:21:
   - n_tokens = 7326
   - truncated = 0

No visible line reports:
- prompt processing completion;
- generation start;
- generated-token timing;
- response completion.

## WOLFRAM CHECK

If the checkpoint `n_tokens=2048` and release `n_tokens=7326` were the same continuously accumulated processed-token metric, then:

- token delta = 5278
- elapsed checkpoint -> release = 72.39 s
- implied average rate = 72.91 tokens/s
- ratio to checkpoint prompt-processing rate = 4.15x

This makes a simple same-metric, same-path interpretation unsafe.

`n_tokens=7326` at release must not be interpreted as "7326 prompt tokens were freshly evaluated" without source proof.

## INTERPRETATION

Current evidence supports:

`REQUEST RECEIVED -> TASK LAUNCHED -> PROMPT PROCESSING OBSERVED -> OUTER BUDGET CANCEL -> RELEASE`

It does not support a claim that generation began.

The next operation is source-level semantics for the two llama-server log sites:
- `print_timing: ... prompt processing`
- `release: ... stop processing: n_tokens`

## NEXT

Inspect installed llama.cpp source for those exact log strings and their surrounding calculations before changing any timeout or prompt configuration.

**Reality is final validator.**
