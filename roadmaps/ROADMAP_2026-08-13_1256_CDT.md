# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 12:56 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1254_CDT.md`

## MATERIAL CHANGE

The attempted v4 runtime-value probe did not recover a `[context-overflow-precheck]` runtime diagnostic. Its visible output is source fallback only.

That is negative evidence:
- the diagnostic line may not have been emitted at the active log level,
- may have been emitted elsewhere,
- or may not survive in the queried log.

Installed source now proves the exact reserve-clamping formula:

`minPromptBudget = min(MIN_PROMPT_BUDGET_TOKENS, max(1, floor(contextTokenBudget * MIN_PROMPT_BUDGET_RATIO)))`

`effectiveReserveTokens = min(requestedReserveTokens, max(0, contextTokenBudget - minPromptBudget))`

`promptBudgetBeforeReserve = max(1, contextTokenBudget - effectiveReserveTokens)`

`overflowTokens = max(0, estimatedPromptTokens - promptBudgetBeforeReserve)`

Route selects compaction when overflow remains after any permitted tool-result reduction.

## CONDITIONAL WOLFRAM CHECK

Under the candidate values already seen elsewhere in installed OpenClaw compaction logic:

- contextTokenBudget = 16384
- MIN_PROMPT_BUDGET_TOKENS = 8000
- MIN_PROMPT_BUDGET_RATIO = 0.5
- requestedReserveTokens = 20000

Wolfram computes:

- minPromptBudget = 8000
- effectiveReserveTokens = 8384
- promptBudgetBeforeReserve = 8000

This would make CLI pre-turn compaction begin pressure handling above an 8000-token prompt budget despite a 16384-token model window.

This is **not yet promoted to root cause** because the constants, requested reserve, and v4 session context/token values still need direct proof in this exact path.

## NEXT

Read-only proof:
1. capture `MIN_PROMPT_BUDGET_TOKENS` and `MIN_PROMPT_BUDGET_RATIO` definitions;
2. capture the implementation/default backing `settingsManager.getCompactionReserveTokens()`;
3. capture v4 session entry values:
   - contextTokens
   - totalTokens
   - totalTokensFresh
4. then calculate the exact available prompt budget;
5. if possible, estimate or recover v4 prompt-token pressure;
6. mutate only after the numerical trigger is proven.

## CURRENT EDGE

`MODEL → PAN → OPENCLAW EXEC → FORGE → RESULT → PAN = PASS`

Blocked:
`CLI PRE-TURN RESERVE CLAMP / PROMPT BUDGET → FORCED COMPACTION → TIMEOUT`

## PHASE

`RESERVE CONSTANTS + V4 SESSION VALUES → NUMERIC ROOT CAUSE → NARROW REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
