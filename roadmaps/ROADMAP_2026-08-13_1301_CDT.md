# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 13:01 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1256_CDT.md`

## MATERIAL CORRECTION

New trajectory evidence separates two compaction events that had been conflated.

### MAIN EMBEDDED RUN

Session:
`agent:main:pan-actuator-v4`

Run id:
`aecfcd67-3109-4209-8c44-98dd50d88627`

Observed:
- exec completed successfully;
- assistant final text was actually generated:
  `assistantTexts = ["STATUS=PASS"]`
- run nevertheless ended:
  - `timedOut = true`
  - `timedOutDuringCompaction = true`
  - `timedOutDuringToolExecution = false`
- earlier lifecycle evidence for this run:
  `embedded_run_compaction_start reason=threshold`

Therefore Pan's post-tool final response generation itself succeeded. The main failure is that a subsequent embedded auto-compaction prevented clean run completion / return.

### GATEWAY FALLBACK / CLI PATH

A separate fallback trajectory begins while the main compaction is still unresolved.

Its later diagnostic uses:
`trigger=cli_budget`

Wolfram verifies the two lifecycles overlap in time. Therefore `cli_budget` is a secondary/fallback compaction path, not sufficient evidence for the root cause of the original embedded-run timeout.

### CONSEQUENCE

The prior working diagnosis that `cli_budget` was the primary initiating subsystem is demoted.

The primary root candidate returns to the **embedded session auto-compaction threshold path**, but not necessarily memory-core preflight.

New source lead already present in evidence:

`if (shouldCompact(contextTokens, contextWindow, settings)) return await this.runAutoCompaction("threshold", false);`

in the session implementation.

This is a distinct candidate from memory-core preflight and from CLI-budget compaction.

## IMPORTANT RUNTIME FACTS

V4 trajectory proves:
- provider = pan-local
- model = pan-qwen
- API = openai-completions
- session contextTokens = 16384
- assistant output `STATUS=PASS` existed before final run failure
- reported aggregate usage:
  - input = 3522
  - cacheRead = 15145
  - output = 136
  - total = 18803

Do not equate aggregate usage directly with the `shouldCompact()` input until source proves that mapping.

## NEXT

Inspect only the primary embedded threshold mechanism:

1. `shouldCompact(contextTokens, contextWindow, settings)` implementation;
2. `DEFAULT_COMPACTION_SETTINGS`;
3. exact session call site around `runAutoCompaction("threshold", false)`;
4. source of `contextTokens`, `contextWindow`, and `settings` at that call;
5. calculate the primary threshold numerically;
6. repair primary auto-compaction;
7. rerun fresh acceptance before touching secondary CLI-budget behavior.

If the main embedded run returns cleanly, the fallback path may never activate.

## CURRENT EDGE

Proven:
`MODEL → PAN → EXEC → FORGE → RESULT → PAN FINAL TEXT = PASS`

Blocked:
`PAN FINAL TEXT → EMBEDDED AUTO-COMPACTION → CLEAN RETURN = FAIL`

## PHASE

`PRIMARY EMBEDDED shouldCompact() TRACE → NARROW REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
