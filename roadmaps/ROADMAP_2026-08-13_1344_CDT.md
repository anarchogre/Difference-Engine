# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 13:44 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1343_CDT.md`

## MATERIAL CHANGE

The cache-to-fresh-prompt transition is now source-resolved in `tools/server/server-context.cpp`.

Observed control flow:

1. `n_past` is established by prior cache/LCP logic.
2. If the entire task prompt is cached, llama-server deliberately decrements `n_past` by one so at least one token is evaluated.
3. `slot.n_prompt_tokens_cache = n_past`.
4. `slot.n_prompt_tokens_processed = 0`.
5. `slot.prompt.tokens.keep_first(n_past)` retains exactly the cached prefix in slot prompt state.
6. For each uncached text token added to the prompt batch:
   - token is appended to `slot.prompt.tokens`
   - `slot.n_prompt_tokens_processed++`
7. Multimodal chunks use the separate bulk increment `+= n_tokens_out`.

For the v6 actuator acceptance prompt, the request is text-only. Therefore, absent another text-path mutation not yet observed, checkpoint state obeys:

`prompt.n_tokens() = n_prompt_tokens_cache + n_prompt_tokens_processed`

At the observed checkpoint:

`prompt.n_tokens() = n_prompt_tokens_cache + 2048`

and logged progress obeys:

`prompt.n_tokens() / task->n_tokens() ≈ 0.78`

The logged `0.78` is rounded to two decimals, so it is not an exact algebraic equality suitable for recovering an exact task-token count.

Wolfram confirms the symbolic relation, under the text-path assumptions:

`2048 + cache = task_tokens * progress`

## INTERPRETATION

The 2048 fresh-token checkpoint occurred after a retained cached prefix had already been counted in `prompt.n_tokens()`.

This explains why progress could be 78% while only 2048 fresh prompt tokens had been evaluated.

Still unresolved:
- exact cached-prefix size `n_past` for task 3142;
- exact task prompt token count;
- whether prompt processing completed and generation began before the 180 s cancellation.

## NEXT

Inspect the immediate post-prompt transition around lines 3550–3610 to identify the exact condition that marks prompt completion and initializes generation.

**Reality is final validator.**
