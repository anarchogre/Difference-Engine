# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 13:13 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1310_CDT.md`

## MATERIAL CHANGE

The installed OpenClaw schema now directly exposes the intended control-plane key:

`agents.defaults.compaction.reserveTokens`

Schema description:
- reserve headroom for reply generation and tool output after compaction;
- higher values for verbose/tool-heavy sessions;
- lower values when retaining more history matters;
- active-model context can cap the effective reserve to preserve usable prompt space.

The embedded-run construction also shows:

`applyAgentCompactionSettingsFromConfig({ settingsManager, ... })`

before the final auto-compaction guard is applied.

Together with the already-proven core SettingsManager getter:

`this.settings.compaction?.reserveTokens ?? 16384`

this is sufficient to proceed to a controlled config repair, provided the existing config is preserved first and the accepted key is verified by the installed CLI/schema during mutation.

## WOLFRAM REPAIR ARITHMETIC

Pan model:
- context window = 16384
- max output tokens = 2048

Candidate reserves:

- reserve 2048:
  - threshold = 14336
  - reserve fraction = 12.5%
  - headroom = 1x configured max output

- reserve 4096:
  - threshold = 12288
  - reserve fraction = 25%
  - headroom = 2x configured max output

- reserve 8192:
  - threshold = 8192
  - reserve fraction = 50%
  - headroom = 4x configured max output

Wolfram result:
`4096` is the smallest candidate that preserves two full configured output budgets.

## REPAIR CHOICE

Promote `reserveTokens = 4096` for the actuator repair trial.

Reason:
- below model window;
- removes zero-threshold defect;
- preserves two configured 2048-token output budgets;
- leaves 12288 tokens for prompt/history before core threshold compaction;
- less aggressive than 8192.

`keepRecentTokens = 20000` remains drift and must be repaired before relying on successful compaction, but it is not required to test whether the zero-threshold lifecycle defect is the actuator blocker.

## NEXT

1. preserve current OpenClaw config;
2. set `agents.defaults.compaction.reserveTokens = 4096`;
3. verify installed config accepts and reads back the value;
4. restart only if the supported config mechanism requires it;
5. run fresh actuator acceptance;
6. require clean final return:
   `STATUS=PASS ACTUATOR_END_TO_END=PASS`
7. if clean, immediately hand control to Pan for ingestion recovery.

## CURRENT EDGE

Root cause:
`CORE DEFAULT RESERVE 16384 == PAN CONTEXT 16384 -> THRESHOLD 0 -> AUTO-COMPACTION ON EVERY POSITIVE TURN`

Repair trial:
`reserveTokens = 4096 -> threshold = 12288`

## PHASE

`RESERVE REPAIR -> ACTUATOR END-TO-END -> INGEST -> BE -> BUILD -> EXTEND -> RECONSTITUTE -> RESEARCH -> ITERATE`

**Reality is final validator.**
