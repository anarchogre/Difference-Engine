# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 13:52 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1351_CDT.md`

## MATERIAL CHANGE

Interactive shell history recovered the exact v6 acceptance launcher.

Recovered v6 invocation:

`openclaw agent --session-key "$SESSION" --model pan-local/pan-qwen --timeout 180 --json --message 'ACTUATOR END-TO-END ACCEPTANCE. Use exec exactly once. Run exactly: /usr/bin/git -C /home/reckless-ideator/Difference-Engine status --short --branch . Do not mutate anything. Return exactly: STATUS=PASS ACTUATOR_END_TO_END=PASS only if exec actually returns the git output.' >"$RUN/acceptance.json" 2>&1`

Therefore the v7 trial can preserve:
- model
- acceptance prompt
- exec target
- JSON mode
- output capture
- session isolation

and change only:
- outer CLI timeout: `180 -> 600`

Provider timeout is already 600 seconds.

Wolfram verifies:
- v6 outer budget: 180 s
- proposed v7 outer budget: 600 s
- multiplier: 3.3333333333
- added outer budget: 420 s

## INTERPRETATION

The exact launcher is now provenance-recovered. No reconstruction is required.

The next experiment is controlled:
`V6 + ONLY(timeout 180 -> 600) = V7`

No provider/model/compaction/prompt/tool-policy change is justified before that trial.

## NEXT

Create a fresh v7 evidence directory and session key, then rerun the recovered acceptance with `--timeout 600`.

**Reality is final validator.**
