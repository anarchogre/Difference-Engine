# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 13:30 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1328_CDT.md`

## MATERIAL CHANGE

The v6 provider request is now proven to have reached `pan-llama-server.service`.

Task:
- llama-server task `3142`
- launch: `13:14:12`
- OpenClaw cancel: `13:17:12`
- release: `13:17:21`

Observed:
- task launched immediately when the OpenClaw request began;
- at `13:16:09`, llama-server was still in prompt processing;
- checkpoint: `n_tokens=2048`, `progress=0.78`, `116.61 s`, `17.56 tokens/s`;
- OpenClaw canceled the task exactly at the 180-second outer run budget;
- llama-server released the task about 9 seconds later;
- no compaction occurred and no tool call began.

Wolfram verifies:
- launch -> checkpoint = 117 s
- checkpoint -> cancel = 63 s
- launch -> cancel = 180 s
- cancel -> release = 9 s

A naive linear interpretation of the checkpoint progress would estimate about 2626 prompt tokens total and prompt completion near 13:16:42, but this must not be promoted because llama-server progress semantics and final `n_tokens=7326` require source/log interpretation.

## INTERPRETATION

The current blocker is now localized below OpenClaw orchestration and above completed model output:

`OpenClaw -> llama-server request = PASS`
`llama-server task launch = PASS`
`prompt processing = SLOW / INCOMPLETE BEFORE OUTER BUDGET`
`model response = NOT PRODUCED BEFORE 180 s`
`OpenClaw cancellation = EXPECTED AT RUN BUDGET`

The 4096 compaction-reserve repair remains valid and unrelated to this v6 timeout.

## NEXT

Inspect every llama-server line for task 3142 before changing timeouts or prompt size. Determine whether:
- prompt evaluation completed;
- generation began;
- cache/LCP reuse changed effective prompt work;
- `n_tokens=7326` represents retained context, processed prompt, or another slot metric.

**Molasses removes impossibilities. Reality is final validator.**
