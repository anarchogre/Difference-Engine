# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 13:48 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1346_CDT.md`

## MATERIAL CHANGE

Slot-level llama-server logs for task 3142 between 13:16:00 and 13:17:22 contain only:

- 13:16:09 prompt-processing checkpoint:
  `n_prompt_tokens_processed = 2048`
  `progress = 0.78`
  `t = 116.61 s`
  `17.56 tokens/s`
- 13:17:21 release:
  `prompt.n_tokens() = 7326`

No slot-level prompt-completion timing, generation timing, or `n_decoded` progress line is present.

## CONSTRAINT

Absence of an `n_decoded` log does not prove zero generated tokens because source logging suppresses generation timing while `n_decoded < 100` and is cadence-gated.

Therefore:
- generation before cancellation remains unproven;
- zero generation cannot be promoted solely from log absence.

## WOLFRAM BOUND

Using:
- displayed progress rounded to 0.78, so true ratio is approximately [0.775, 0.785);
- checkpoint `prompt.n_tokens()` cannot exceed later release value 7326;

the total task prompt is bounded above by approximately 9452 tokens.

This is a coarse upper bound only. Exact task-token count is unresolved.

At the observed prompt-eval rate 17.56 tokens/s, a 180-second outer budget is demonstrably tight for a several-thousand-token fresh prompt path.

## INTERPRETATION

The limiting layer has now been identified sufficiently to justify changing the test harness run budget:

`OpenClaw request delivery = PASS`
`llama-server task launch = PASS`
`prompt processing = ACTIVE`
`180 s outer run budget = EXPIRES BEFORE CLEAN RESPONSE`
`compaction = NOT INVOLVED`

Provider configuration already permits 600 seconds; the failed v6 acceptance itself imposed a 180-second outer run budget.

The next operation is not to change model/provider configuration. It is to recover the exact v6 acceptance invocation and rerun the same acceptance with a larger outer test budget, preserving all other variables.

## NEXT

Inspect v6 acceptance evidence to recover the exact command line and timeout argument. Then create v7 with only the outer run-budget variable changed.

**Reality is final validator.**
