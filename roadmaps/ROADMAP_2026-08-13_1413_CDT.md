# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 14:13 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1412_CDT.md`

## MATERIAL CHANGE

Managed Node runtime is directly verified:

`/home/reckless-ideator/.openclaw/tools/node-v24.15.0/bin/node --version`
→ `v24.15.0`

Wolfram confirms:
- managed Node verified;
- direct managed-Node invocation removes the CLI shim's `/usr/bin/env node` PATH dependency;
- v7 outer timeout remains 600 seconds.

## CONTROLLED V7 RETRY

Preserve the second launcher failure capture before overwriting `acceptance.json`.

Then invoke:

`managed node -> openclaw.mjs -> agent`

using:
- same v7 session key;
- same model;
- same prompt;
- same git command;
- same JSON output;
- same 600-second outer timeout.

Relative to the successful v6 OpenClaw launch, the intended experiment remains:
`--timeout 180 -> --timeout 600`.

## NEXT

Run the direct managed-Node v7 acceptance.

**Reality is final validator.**
