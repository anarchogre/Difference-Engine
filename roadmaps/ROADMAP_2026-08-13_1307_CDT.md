# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 13:07 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1305_CDT.md`

## ROOT CAUSE VALIDATED

Installed OpenClaw 2026.7.1-2 source and persisted state now prove:

- core SettingsManager default:
  - compaction enabled = true
  - reserveTokens = 16384
  - keepRecentTokens = 20000
- no persisted compaction override was found in OpenClaw state by the targeted state census
- Pan v4 session context window = 16384
- primary embedded auto-compaction predicate:
  `contextTokens > contextWindow - reserveTokens`

Therefore effective primary threshold:

`16384 - 16384 = 0`

and the trigger becomes:

`contextTokens > 0`

Wolfram independently verifies:
- threshold = 0
- zero tokens do not trigger
- every positive context token count triggers

This exactly explains the observed primary lifecycle:
- tool execution succeeds
- Pan generates `STATUS=PASS`
- embedded auto-compaction starts with `reason=threshold`
- compaction times out
- otherwise-successful run is returned as failure

## ROOT CAUSE

**OPENCLAW CORE COMPACTION RESERVE EQUALS PAN MODEL CONTEXT WINDOW**

The 16384-token default reserve is incompatible with Pan's 16384-token model context.

`keepRecentTokens = 20000` is also larger than the model window and should be treated as secondary configuration drift, but it is not required to explain the threshold trigger.

## NEXT

Before mutation, prove the control-plane binding from `agents.defaults.compaction.reserveTokens` to the core SettingsManager value used by the session. Then:

1. preserve current config;
2. set a bounded reserve below the model window;
3. restart only if required by the supported config mechanism;
4. create fresh actuator session;
5. run one read-only git status;
6. require clean final return:
   `STATUS=PASS ACTUATOR_END_TO_END=PASS`

Candidate reserve values are not yet promoted. A 4096 reserve would place the threshold at 12288; a 2048 reserve at 14336. Selection must follow source/config semantics and model output budget, not convenience.

## CURRENT EDGE

`MODEL → PAN → EXEC → FORGE → RESULT → PAN FINAL TEXT = PASS`

Blocked:
`INVALID CORE COMPACTION RESERVE → THRESHOLD ALWAYS TRUE → COMPACTION TIMEOUT`

## PHASE

`CONTROL-PLANE BINDING PROOF → RESERVE REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
