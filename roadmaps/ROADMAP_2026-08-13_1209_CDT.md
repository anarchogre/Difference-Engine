# DIFFERENCE ENGINE ROADMAP
## 2026-08-13 12:09 CDT

**Artifact class:** ROADMAP
**Epoch:** II
**Status:** LOCKED SNAPSHOT
**Predecessor:** `ROADMAP_2026-08-13_1208_CDT.md`

## MATERIAL CHANGE

Installed OpenClaw 2026.7.1-2 schema now resolves the accepted `agents.defaults.compaction` object.

Confirmed accepted keys:
- `mode`: `default | safeguard`
- `provider`
- `reserveTokens`
- `keepRecentTokens`
- `reserveTokensFloor`
- `maxHistoryShare`
- `customInstructions`
- `identifierPolicy`
- `identifierInstructions`
- `recentTurnsPreserve`
- `qualityGuard.enabled`
- `qualityGuard.maxRetries`
- `midTurnPrecheck.enabled`
- `postIndexSync`
- `postCompactionSections`
- `model`
- `timeoutSeconds`
- `memoryFlush.enabled`
- `memoryFlush.model`
- `memoryFlush.softThresholdTokens`
- `memoryFlush.forceFlushTranscriptBytes`
- `memoryFlush.prompt`
- `memoryFlush.systemPrompt`
- `truncateAfterCompaction`
- `maxActiveTranscriptBytes`
- `notifyUser`

Correction:
- `softThresholdTokens` is not a top-level compaction threshold control.
- It belongs under `agents.defaults.compaction.memoryFlush.softThresholdTokens`.
- The previously attempted `agents.defaults.compaction.enabled` is not part of this installed schema.

`responsesCompactThreshold` remains unresolved and must be traced from its source-resolution call before any configuration mutation.

## CURRENT EDGE

Actuator primitive:
`MODEL → PAN → OPENCLAW EXEC → FORGE → RESULT → PAN = PASS`

Remaining:
`PAN POST-TOOL RESPONSE → CLEAN TERMINATION = BLOCKED`

## NEXT

Read-only source trace:
1. inspect `resolvePositiveIntegerParam(..., "responsesCompactThreshold")`
2. identify all `sources` passed to it
3. identify the configuration/environment/CLI path, if any
4. inspect the threshold-state call site and context-window arithmetic
5. determine why a fresh one-tool session crossed threshold

No config change until that path is proven.

## PHASE

`RESPONSES COMPACTION SOURCE TRACE → RESPONSE-LIFECYCLE REPAIR → ACTUATOR END-TO-END → INGEST → BE → BUILD → EXTEND → RECONSTITUTE → RESEARCH → ITERATE`

**Reality is final validator.**
